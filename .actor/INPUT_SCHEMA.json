{
  "title": "PRCA Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "URLs to start scraping from",
      "prefill": [
        "https://www.prorodeo.com/results"
      ],
      "editor": "stringList"
    },
    "maxRequestsPerCrawl": {
      "title": "Max requests per crawl",
      "type": "integer",
      "description": "Maximum number of pages to crawl (0 for unlimited)",
      "minimum": 0,
      "default": 100,
      "unit": "requests"
    },
    "maxConcurrency": {
      "title": "Max concurrency",
      "type": "integer",
      "description": "Maximum number of pages processed in parallel",
      "minimum": 1,
      "maximum": 20,
      "default": 5
    },
    "onlyCompleted": {
      "title": "Only Completed Events",
      "type": "boolean",
      "description": "Only scrape events with 'Completed' status",
      "default": true
    },
    "includeDaysheets": {
      "title": "Include Daysheets",
      "type": "boolean",
      "description": "Also scrape daysheet data for each event",
      "default": false
    },
    "debug": {
      "title": "Debug mode",
      "type": "boolean",
      "description": "Enable debug logging",
      "default": false
    },
    "useProxy": {
      "title": "Use Proxy",
      "type": "boolean",
      "description": "Enable residential proxy rotation to avoid blocking (requires Apify proxy subscription)",
      "default": false
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings (leave empty to use default residential proxies)",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    }
  },
  "required": []
}
